<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>FlexChain Fitness</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <nav class="nav">
      <div class="brand">FlexChain Fitness</div>
      <div class="nav-links">
        <a class="active" href="/">Home</a>
        <a href="/about">About Us</a>
        <a href="/account">Wallet / Account</a>
        <a href="/profile">Profile</a>
        <a href="/login">Login</a>
        <a href="/register">Register</a>
      </div>
      <div class="nav-actions">
        <a class="button ghost" href="/profile">Profile</a>
        <button class="button ghost" id="connectWallet">Connect Wallet</button>
      </div>
    </nav>

    <header class="hero">
      <div>
        <p class="eyebrow">On-chain gym membership</p>
        <h1>Train smarter with transparent blockchain payments.</h1>
        <p class="lede">
          Every membership signup triggers automated payment confirmation,
          loyalty point issuance, and NFT membership upgrades without third-party trust.
        </p>
      </div>
      <div class="hero-card">
        <h3>Wallet status</h3>
        <p id="walletAddress" class="mono">Connected: <%= account || 'Not connected' %></p>
        <p class="muted">Use MetaMask to pay with test ETH and earn gym rewards.</p>
      </div>
    </header>

    <main class="container">
      <section class="section">
        <div class="section-header">
          <div>
            <h2>Membership subscriptions</h2>
            <p class="muted">Pick daily, weekly, monthly, or yearly access and start earning tier rewards.</p>
          </div>
          <span class="badge">Blockchain verified</span>
        </div>

        <% if (products && products.length) { %>
          <div class="grid">
            <% products.forEach((product) => { %>
              <article class="product-card">
                <div class="product-image" aria-hidden="true"></div>
                <h3><%= product.name %></h3>
                <p class="muted"><%= product.description %></p>
                <div class="product-meta">
                  <span class="price"><%= (Number(product.priceWei) / 1e18).toFixed(3) %> ETH</span>
                  <a class="button ghost" href="/product/<%= product.id %>">View plan</a>
                </div>
              </article>
            <% }) %>
          </div>
        <% } else { %>
          <p class="muted">Membership plans will appear after contracts are deployed.</p>
        <% } %>
      </section>
    </main>

    <footer class="footer">
      <span>FlexChain Fitness c 2026</span>
      <span>Powered by Web3, Loyalty Tokens, and NFT Membership</span>
      <span class="server-status">Listening at <%= serverUrl || 'http://localhost:3000' %></span>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/web3@1.10.0/dist/web3.min.js"></script>
    <script src="/js/dapp.js"></script>
  </body>
</html>
