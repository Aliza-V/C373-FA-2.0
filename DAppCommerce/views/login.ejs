<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Member Login</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <nav class="nav">
      <div class="brand">FlexChain Fitness</div>
      <div class="nav-links">
        <a href="/">Home</a>
        <a href="/about">About Us</a>
        <a href="/account">Wallet / Account</a>
        <a href="/profile">Profile</a>
        <a class="active" href="/login">Login</a>
        <a href="/register">Register</a>
      </div>
      <div class="nav-actions">
        <a class="button ghost" href="/profile">Profile</a>
        <button class="button ghost" id="connectWallet">Connect Wallet</button>
      </div>
    </nav>

    <main class="container">
      <section class="card auth-card">
        <h1>Member login</h1>
        <p class="muted">Sign in with your email and password. Connect MetaMask after login to pay.</p>
        <form id="loginForm" class="auth-form">
          <label class="field">
            <span>Email</span>
            <input id="loginEmail" type="email" placeholder="you@example.com" required />
          </label>
          <label class="field">
            <span>Password</span>
            <input id="loginPassword" type="password" placeholder="••••••••" required />
          </label>
          <div class="auth-actions">
            <button class="button" type="submit">Login</button>
            <a class="button ghost" href="/register">Register instead</a>
          </div>
        </form>
        <p class="muted">Current session</p>
        <p id="sessionEmail" class="mono">Not logged in</p>
        <div class="notice" id="authNotice" hidden></div>
      </section>
    </main>

    <footer class="footer">
      <span>FlexChain Fitness c 2026</span>
      <span>Secure email login</span>
      <span class="server-status">Listening at <%= serverUrl || 'http://localhost:3000' %></span>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/web3@1.10.0/dist/web3.min.js"></script>
    <script src="/js/dapp.js"></script>
  </body>
</html>
